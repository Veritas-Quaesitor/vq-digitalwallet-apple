# CLAUDE.md — vq-digitalwallet-apple

This file is read automatically by Claude Code at session start. It is the authoritative
restore point for any new session on this project.

---

## Project Identity

- **npm package name:** `vq-digitalwallet-apple`
- **Class name:** `VqDigitalWalletApple`
- **Version:** `1.1.0`
- **Author:** Veritas Quaesitor
- **License:** MIT
- **Local path (after folder rename):** `c:\Users\fvaneeden\source\vq-digitalwallet-apple\`
- **GitHub remote (once created):** `https://github.com/Veritas-Quaesitor/vq-digitalwallet-apple`
- **npm registry:** `https://www.npmjs.com/package/vq-digitalwallet-apple`

---

## Architecture (do not change)

- **Payment flow:** W3C Payment Request API + Apple Pay JS SDK loaded from CDN
- **NOT ApplePaySession** — the Payment Request API approach is intentional and must be kept
- **Build:** Rollup producing two outputs:
  - `vqdigitalwalletapple.js` — IIFE, unminified, for CDN/browser `<script>` tag
  - `dist/vqdigitalwalletapple.min.js` — UMD, minified, for npm/bundlers
- **CDN URL:** Pinned to `https://applepay.cdn-apple.com/jsapi/v1.3.7/apple-pay-sdk.js`
- **SRI hash:** `sha384-m79g5rSF+8ObiCeY5RGEXjDpxURj/6A4rz0xXbEY/UEzLITgAWZ0+x3gcYjVEuVe`
- **Zero runtime dependencies** — all packages are devDependencies only

---

## Current State (as of HEAD `55ff86a`)

- 43/43 tests passing
- 21 OWASP/CWE security tests covering: CWE-20, CWE-79, CWE-116, CWE-346, CWE-400, CWE-613, CWE-770, CWE-1321
- 0 ESLint errors
- 0 npm audit vulnerabilities
- Working tree clean

### Commit history
```
55ff86a  security: harden OWASP findings 2, 6, 7, 9, 13 (43 tests, 21 CWE)
2b25fbf  Security hardening: 8 CWE remediations from independent audit
b608dd3  Rebrand EpsApplePay → vq-digitalwallet-apple v1.1.0 for public release
8309438  fixed spelling mistake
1381a74  added actual error code to README
6cd3c10  Initial commit Apple Pay SDK
```

---

## Key Files

| File | Purpose |
|---|---|
| `src/vqdigitalwalletapple.js` | Single source file — all logic lives here |
| `vqdigitalwalletapple.js` | IIFE build output (CDN, unminified) — regenerated by `npm run build` |
| `dist/vqdigitalwalletapple.min.js` | UMD build output (npm, minified) — regenerated by `npm run build` |
| `vqdigitalwalletapple.d.ts` | TypeScript definitions |
| `test/vqdigitalwalletapple.test.js` | Full test suite (43 tests) |
| `test/setup.js` | Jest jsdom mocks (PaymentRequest, crypto, fetch, querySelector, etc.) |
| `rollup.config.js` | Build configuration |
| `jsdoc.conf.json` | JSDoc config — uses `docdash` template (NEVER better-docs) |
| `docs/` | Generated JSDoc output — committed for GitHub Pages |

---

## Build Commands

```bash
npm run build       # IIFE + UMD outputs
npm test            # 43 tests via Jest
npm run lint        # ESLint on src/
npm run docs        # Regenerate docs/ with docdash
npm run test:coverage  # Coverage report
```

The `prepublishOnly` script runs `clean → build → test → lint` automatically before any `npm publish`.

---

## Hard Constraints (never violate)

- **NEVER use `better-docs`** as JSDoc template — it has HIGH/MODERATE vulnerabilities. Always use `docdash`.
- **NEVER hardcode real merchant IDs, API keys, Apple certificates, or credentials** in any file.
- **NEVER switch to ApplePaySession** — the Payment Request API architecture is a deliberate decision.
- **Demo page** (`demo/index.html`) is deferred indefinitely — do not create it.
- **`dist/`** is gitignored (not committed) but IS in the npm `files` array (published to npm).
- **`.claude/`** is gitignored and must never be committed.

---

## What Was Done (full history)

1. **Rebrand** (`b608dd3`): Renamed all files and identifiers from `EpsApplePay`/`Ecentric` to `VqDigitalWalletApple`/Veritas Quaesitor. Updated all config files, LICENSE, README, TypeScript defs, docs.
2. **Security round 1** (`2b25fbf`): 8 CWE remediations — removed Math.random fallback (CWE-330), extended sanitizeString (CWE-116), merchantIdentifier format validation (CWE-20), card network allowlist (CWE-285), description sanitization (CWE-1287), HTTP status removed from error messages (CWE-200), timeout wires PaymentRequest.abort() (CWE-404), defaults object frozen.
3. **Security round 2** (`55ff86a`): isTrusted guard on merchantvalidation (CWE-346), CDN URL pinned to v1.3.7 with verified SRI (CWE-732), rate limiter documented as UX-only (CWE-285), consumeToken() consume-once method (CWE-613), pre-injected script SRI verification (CWE-613).

---

## What Is Next

1. ~~Create CLAUDE.md~~ ✅
2. Create `CONTRIBUTING.md`
3. Rename local folder: `Ecentric.ApplePay.ClientSdk` → `vq-digitalwallet-apple`
4. Create GitHub remote repo (`Veritas-Quaesitor/vq-digitalwallet-apple`, public) and push
5. Configure GitHub Pages to serve from `docs/` on `main`
6. Apply branch protection on `main` (no force push, no delete)
7. `npm publish`
